<% if @carted_product.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@carted_product.errors.count, "error") %> prohibited this carted_product from being saved:</h2>
 
    <ul>
    <% @carted_product.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>